Backend source modules:

- `app/` orchestration and service entry points
- `contracts/` public API response and domain types
- `db/` Postgres config, schema bootstrap, and repositories
- `providers/` upstream Bayse and Polymarket adapters
- `routes/` Fastify HTTP routes
