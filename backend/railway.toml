[build]
builder = "RAILPACK"
buildCommand = "npm run railway:build:backend"
watchPatterns = ["/backend/**"]

[deploy]
startCommand = "env SMART_MONEY_SCHEDULER_ENABLED=false npm run railway:start:backend"
healthcheckPath = "/health"
